# CQN-AS Value 学习问题:诊断、改进与最新结果

**任务环境**:BiGym MovePlate,像素输入,51 条成功人类演示;CQN-AS 官方结构(coarse-to-fine 3 层 × 每维 5 bins,C51 51 atoms,动作序列 K=16,动作维度 D=15,temporal ensemble 闭环执行)。
**评测纪律**:所有主张基于**密封评估**(50 episodes、评测前从未接触过的全新 eval seed 集、双协议:最近验证最优快照 + 最终快照)或**预注册判据**;25-episode validation 数字只用于中途决策,报告中会明确标注口径。

---

## 一、核心结论(TL;DR)

1. **原版 CQN-AS 没有学到真实 value**:它的 Q 函数与自身行为的真实回报**负相关**(ρ ≈ −0.45 ~ −0.54),对没执行过的动作的排序停在随机水平(反事实准确率 ≈ 0.57,机会线 0.5)。这一结论在 10.5k 与 100k 两个训练尺度、共 8 个独立训练 seed 上复现。Q 实际学到的是"认出专家的动作"——伪装成 RL 的模仿。
2. **改动一(算法,已密封)**:coarse-flow(CCFF)——TD 只保留在统计上可识别的粗层,细层由 bin 条件 flow 输出连续残差。密封结果 **78.0/80.0 vs 原版 68.0/63.3(+10.0/+16.7pp,3/3 seed 一致,双协议)**,是本程序首个存活密封的正收益。消融表明该增益来自层级分工的架构效应(margin 驱动的粗层选择 + 连续残差),而非 TD。
3. **改动二(训练方案,已定稿)**:分层 ε-bin 探索 × margin 权重衰减 × 100k 步。2×2 因子分解(每格 2 训练 seed)+ 200-episode 高精度评估的终版结论:**两因素在任务与 value 两个口径上都是超可加交互**——组合配方任务 75.0% vs 官方 64.6%(+10.4pp),而单因素只有 +4.2 / +0.2;反事实探针仅组合臂离开机会线(0.644 vs 0.567,双 seed 一致)。机制:探索制造"同状态、异 bin"的对照数据,margin 衰减解除广播压制让 TD 消费之,缺一不可。

---

## 二、"原版没学到 value" 的证据与实验细节

### 证据 1:Q-回报校准审计(value fidelity audit)

**问题**:Q(s, a_执行) 是否能预测执行该动作后的真实回报?这是对 value 最低限度的要求——甚至不涉及反事实。

**方法细节**:
- 从训练 run 的 replay(episode 级 .npz)中分层抽样四组:demo 成功 / demo 失败 / online 成功 / online 失败,每组内跨 episode 轮转取锚点,避免单条长轨迹主导;
- 对每个锚点,计算 checkpoint 的 canonical Q(执行动作的深层期望 Q)与两种地面真值:折扣原始回报、first-success 折扣回报(γ=0.99);
- 统计 Spearman 秩相关(整体与分组内)。

**结果**:
| 训练尺度 | 配置 | ρ(Q, 回报) 整体 | demo 成功组内 | 判读 |
|---|---|---|---|---|
| 10.5k | gate 平台 | **−0.54** | ~0.7 | 反校准 |
| 100k × 4 seeds | 官方(self-imitation 开) | −0.06 / −0.67 / −0.37 / −0.72(均值 **−0.45**) | 0.60–0.71 | 反校准复现 |

**判读**:Q 在 demo 组内的排序不错(0.6–0.7)——它认得"哪个更像专家";但对自身行为的真实结果,整体是负相关。**十倍在线数据 + 自模仿不能修复。**

### 证据 2:反事实探针(sibling counterfactual probe)

**问题**:Q 对**没执行过的动作**有没有知识?这是 RL 区别于模仿的核心。

**方法细节**:
- 在 12 个固定 eval seed 的轨迹上取锚点(第 30/75/120 步);
- **干预**:强制把策略在粗层(level 0)选中的 bin 换成一个随机兄弟 bin(维度轮转选取),连续执行 4 步后交还策略,实测该反事实分支的真实回报;与不干预分支对比,得到"哪个 bin 实际更好"的地面真值;
- 问 critic:Q 认为哪个 bin 更好?统计**符号一致率**(pairwise sign accuracy),只计入实际回报有差异的"信息对";state-bootstrap 10000 次给出置信区间;
- 干预在整数 bin 格上平移、保留 bin 内偏移,重新编码后精确落回目标 zoom 路径(alias-free,干预即所测)。

**结果**:
| checkpoint | 准确率(信息对数) |
|---|---|
| 10.5k 各变体(多轮测量) | 0.54–0.62 |
| 100k 官方 × 4 seeds | 0.569 / 0.579 / 0.537 / 0.583,合并 **0.567**(716 对) |

**判读**:全部在机会线(0.5)附近的窄带内。**Q 对没走的路一无所知,且数据量 ×10 无改善。**

### 证据 3:机制定位(为什么会这样)

- **margin 损失是广播式的**:在每个 demo 状态,它把*所有*非选中 bin 的 Q 往下压——非选中 bin 的值由模仿信号雕刻,而非结果数据;
- **数据缺同状态异动作的对照**:行为近贪心,同一状态反复选同一 bin(高斯噪声 σ 远小于粗 bin 宽 0.4);online 数据有 episode 间的成败变异(所以"行为价值"可学——我们曾用冻结特征 + 小 MLP 事后回归得到 ρ=0.85–0.92),但没有动作级对照(所以反事实不可学);
- **信用分配被结构破坏**:稀疏终局奖励 + K=16 动作块 + TD target 广播到全部 16×15 个头,episode 级失败无法定位到 bin 级选择;
- **可识别性随层级几何衰减**:粗层(5 bins/维)的兄弟 bin 尚有数据支撑;细层(等效 125 格/维)的兄弟从未被访问——对全层级同质施加 TD 是在要求它学统计上不可学的东西。

### 证据 4(反向验证):控制实验排除了"改法不对"以外的解释

在得出上述结论前,以下修复尝试均以 matched、多 seed、预注册方式失败:共享头加真值锚(剂量-反应:锚权重 0→0.1,成功率 72%→32%);解耦平台学出校准 value 后用于选动作(38.7% / 0%);AWR/自模仿;开环执行;双头不同损失;仅探索不衰减 margin(下文)。

---

## 三、我们做了什么改动,最新结果如何

### 改动 A:coarse-flow 算法(CCFF)——已密封定案

**设计原则**:按可识别性给层级分工。critic 只保留粗层(levels=1,每维 5 bins,损失与原版逐位一致),负责选 bin;删除细层,换成一个**以选中 bin 为条件**的 flow 头(demo-only 条件流匹配,EMA 权重 rollout),在 [-1,1] 的 bin 内坐标里输出连续残差——构造上动作不可能越出 critic 选的格子。精度由 flow 负责(无量化上限),选择由 critic 负责。

**密封结果**(50-ep,全新 eval seeds,成对同训练 seed,双协议):

| 协议 | CCFF | 原版 clean | 成对差 |
|---|---|---|---|
| 最近验证最优 | 80/68/86 → **78.0** | 72/56/76 → 68.0 | +8/+12/+10 → **+10.0** |
| 最终快照 | 88/72/80 → **80.0** | 66/54/70 → 63.3 | +22/+18/+10 → **+16.7** |

预注册优越判据(≥+8pp 且 3/3 seed 同向)双协议同时满足。另:官方配置跑满 100k 的最终成功率均值仅 60%——CCFF 用十分之一预算超过它。

**消融分解**(同 seed 集密封,合并口径):CCFF 79.0 > TD-off(去 Bellman)75.0 > 纯 flow(去选择)67.7 ≈ clean 65.7。判读:flow 残差单独 ≈ 0;**粗层选择机制承重(+11.3)**;选择内部 margin 份额 +7.3、TD 份额 +4.0(seed 间不稳,未达显著)。**诚实定性:CCFF 的增益来自层级分工的架构效应,主导信号仍是模仿——它解决了"怎么把 CQN 变强",还没解决"让 CQN 学会 value"。**

### 改动 B:让 CQN 真的学 value——ε-bin 探索 × margin 衰减 × 尺度(进行中)

**思路**:证据 3 指出缺口是(i)同状态异动作的对照数据、(ii)margin 广播的压制、(iii)数据量。此前的实验各缺一角所以全部 null:仅探索(10.5k,margin 不衰减)null;仅加步数(100k,无探索)null。Stage-158 首次三者齐备:官方 100k 配置 + 分层 ε-bin 探索(逐层 [0.002/0.004/0.008],翻转在 16 个新 plan 上持续、闭环 ensemble 保持)+ margin 权重线性衰减 1.0→0.25。

**终版结果**(2×2 因子 × 2 训练 seed;任务口径 = 200-episode、全新 eval seeds、终点 checkpoint、免选择):

| 臂 | 任务成功率(200-ep) | 反事实探针(双 seed 合并) |
|---|---|---|
| 官方(都不加,4 seeds) | 64.6 | 0.567 |
| 只探索 | 68.8(74.0/63.5) | 0.52(机会线) |
| 只衰减 | 64.8(63.5/66.0) | 0.57(基线) |
| **组合(探索+衰减)** | **75.0(76.0/74.0)** | **0.644(0.655/0.630)** |
| 组合+低维遮罩 | 69.5(70.0/69.0) | 0.52(遮罩摧毁反事实读出) |

**三点定论**:(1) 任务 +10.4pp > 单因素之和 +4.4pp,value 仅组合臂抬升——两口径同构的超可加交互,机制叙事(探索造对照数据、衰减放行 TD)最终成立;(2) 低维遮罩对该配方双口径净损(但双 seed 高度可复现,负结论干净);(3) 方法论:50-ep 评估的 ±7pp 噪声曾造成多次假"冠军"(单臂 84 vs 62 的 seed 集"矛盾"经 200-ep 判定全为抽样噪声),关键对比一律 200-ep。

---

## 四、局限与下一步

- 全部结果来自单任务(MovePlate)、51 demos;CCFF 的外部效度需要第二个 BiGym 任务验证(发表刚需,排队中);
- 改动 B 目前是 2 seeds 的中间点;终点若确认,还需 3-seed 复制 + 密封;
- demo 匮乏轴(9 条 demo)上 CCFF 与原版同样受限(6 seeds,差 +1.4pp)——数据下限问题两者都未解决。

*全部实验记录、预注册与原始数字见 `cqn-flow.md` §22–§39。*
