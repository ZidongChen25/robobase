# @package _global_

# EDP has no JAX implementation in this repository. Keep the config group so
# old Hydra commands fail in create_agent with a precise supported-method list,
# without importing the removed Torch backend.
method:
  name: edp
  is_rl: true
  unsupported_reason: JAX implementation not available
