# @package _global_

# Stage 4 treatment: floor the two largest replay-unseen bins per C2F head.
defaults:
  - cqn_as_pixel_bigym_nobc_mc_top1_floor_gate
  - _self_

method:
  unseen_return_floor_reduction: topk
  unseen_return_floor_topk: 2

hydra:
  run:
    dir: ./exp_local/cqn_no_bc/stage4_mc_top2_floor_${now:%Y%m%d%H%M%S}
