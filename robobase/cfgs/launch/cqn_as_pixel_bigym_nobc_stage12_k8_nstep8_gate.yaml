# @package _global_

# Stage 12 treatment: the eight-step replay return and bootstrap state now
# correspond to the exact eight-action plan executed between replans.
defaults:
  - cqn_as_pixel_bigym_nobc_stage12_k8_nstep1_control
  - _self_

replay:
  nstep: 8

hydra:
  run:
    dir: ./exp_local/cqn_no_bc/stage12_k8_nstep8_${now:%Y%m%d%H%M%S}
