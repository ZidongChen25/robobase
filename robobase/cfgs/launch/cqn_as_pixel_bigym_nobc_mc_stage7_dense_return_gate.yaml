# @package _global_

# Stage 7 treatment: one dense categorical return target over all action bins.
defaults:
  - cqn_as_pixel_bigym_nobc_mc_stage7_max_floor_gate
  - _self_

method:
  dense_return_q_target: true
  unseen_return_floor_weight: 0.0

hydra:
  run:
    dir: ./exp_local/cqn_no_bc/stage7_mc_dense_return_${now:%Y%m%d%H%M%S}
