# @package _global_

# Stage-146b (flow+CQN line): single variable over Stage-146 v1c M=8 --
# EMA weights (decay 0.999) for rollout sampling, to remove the late-run
# flow-BC collapse observed in 2/3 seeds (cqn-flow.md 29.8).
defaults:
  - cqn_as_pixel_bigym_stage146_flow_rerank_gate
  - _self_

method:
  flow_policy_ema: 0.999

hydra:
  run:
    dir: ./exp_local/cqn_stage146_flow_rerank/bigym_${env.task_name}_flowema_${now:%Y%m%d%H%M%S}
