# @package _global_

# Stage 38: add only dense reward-Q action coverage to the official-batch,
# baseline-matched Stage-36 No-BC offline-to-online treatment.
defaults:
  - cqn_as_pixel_bigym_stage36_offline_nobc_candidate256_gate
  - _self_

method:
  dense_return_q_target: true

hydra:
  run:
    dir: ./exp_local/cqn_no_bc/stage38_offline_dense256_${now:%Y%m%d%H%M%S}
