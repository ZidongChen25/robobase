# @package _global_

# Stage 40 online phase: start from a Stage-38 dense reward-Q offline
# checkpoint, then use the canonical reward-derived C51 Bellman target online.
# The runner supplies the frozen raw-10k checkpoint and exact replay contents.
defaults:
  - cqn_as_pixel_bigym_stage36_offline_nobc_candidate256_gate
  - _self_

method:
  dense_return_q_target: false

hydra:
  run:
    dir: ./exp_local/cqn_no_bc/stage40_online_canonical_handoff_${now:%Y%m%d%H%M%S}
