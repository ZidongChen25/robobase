# @package _global_

defaults:
  - ../env: null
  - ../method: ppo

pixels: false
frame_stack: 1
action_repeat: 1
replay_size_before_train: 0
update_every_steps: 1
replay:
  nstep: 1

hydra:
  run:
    dir: ./exp_local/state_ppo/dmc_${env.task_name}_${now:%Y%m%d%H%M%S}
