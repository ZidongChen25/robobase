# @package _global_

# Stage 5 matched control: 16 ordinary-replay plus 16 demo-replay samples.
defaults:
  - cqn_as_pixel_bigym_nobc_mc_top1_floor_gate
  - _self_

batch_size: 16
demo_batch_size: 16

replay:
  demo_only_updates: false

hydra:
  run:
    dir: ./exp_local/cqn_no_bc/stage5_mc_mixed_replay_${now:%Y%m%d%H%M%S}
