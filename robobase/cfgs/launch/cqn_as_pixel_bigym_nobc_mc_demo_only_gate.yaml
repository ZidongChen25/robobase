# @package _global_

# Stage 5 treatment: the same 32-sample update comes entirely from the
# dedicated successful-expert replay. Online collection does not enter loss.
defaults:
  - cqn_as_pixel_bigym_nobc_mc_mixed_replay_gate
  - _self_

demo_batch_size: 32

replay:
  demo_only_updates: true

hydra:
  run:
    dir: ./exp_local/cqn_no_bc/stage5_mc_demo_only_${now:%Y%m%d%H%M%S}
