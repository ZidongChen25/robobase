# @package _global_

# Stage 6 treatment: pure-Q causal conditioning across action dimensions.
defaults:
  - cqn_as_pixel_bigym_nobc_mc_parallel_dims_gate
  - _self_

method:
  autoregressive_action_dims: true

hydra:
  run:
    dir: ./exp_local/cqn_no_bc/stage6_mc_autoregressive_dims_${now:%Y%m%d%H%M%S}
