# @package _global_

# Stage 19: policy-invariant 2x scaling of immediate rewards, MC returns, and
# the learned dense categorical Q representation.
defaults:
  - cqn_as_pixel_bigym_nobc_stage10_discounted_dense_control
  - _self_

method:
  q_reward_scale: 2.0
  dense_return_expected_q_loss: false
  dense_return_finest_neighbor_weight: 0.0
  ordered_success_return_mix: 0.0

hydra:
  run:
    dir: ./exp_local/cqn_no_bc/stage19_reward_scale_${now:%Y%m%d%H%M%S}
