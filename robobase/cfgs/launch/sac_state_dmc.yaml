# @package _global_

defaults:
  - ../env: null
  - ../method: sac

pixels: false
frame_stack: 1
action_repeat: 1
update_every_steps: 1
replay:
  nstep: 1

hydra:
  run:
    dir: ./exp_local/state_sac/dmc_${env.task_name}_${now:%Y%m%d%H%M%S}
