# @package _global_

# Agent line Stage A19 online: the sibling project's winning Stage-42 recipe
# (positive-return-only dense targets + frozen expert replay) on the same
# wide C51 support.
defaults:
  - cqn_as_pixel_bigym_stage42_fixed_expert_replay_gate
  - _self_

method:
  v_min: -29.0
  v_max: 29.0
  atoms: 101

hydra:
  run:
    dir: ./exp_local/cqn_no_bc/agent_a19_online_${now:%Y%m%d%H%M%S}
